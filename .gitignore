# Dados gerados por `curso setup` — reprodutíveis a partir de dados/gerar.py
dados/brutos/
dados/*.duckdb
dados/*.duckdb.wal

# Seu progresso e histórico de respostas (local, não versionado)
.curso/

# Suas respostas. Se quiser versionar seu próprio aprendizado,
# comente a linha abaixo — o repo funciona dos dois jeitos.
respostas/

# Interface web: dependências e build são artefatos
app/node_modules/
app/dist/
app/test-results/
app/playwright-report/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
app/*.tsbuildinfo

# JF Treino — banco local, dependências e build
treino/jf.db
treino/web/node_modules/
treino/web/dist/
treino/web/*.tsbuildinfo
